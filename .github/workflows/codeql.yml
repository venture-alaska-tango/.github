name: CodeQL Analysis Support

on:
  workflow_dispatch:

jobs:
  codeql-support:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: CodeQL analysis support
        run: echo "This workflow exists to support CodeQL analysis of GitHub Actions code"
